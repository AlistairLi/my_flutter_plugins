name: google_adid
description: A Flutter plugin for retrieving Google Advertising ID on Android devices.
version: 0.0.1
repository: https://github.com/AlistairLi/my_flutter_plugins/tree/main/plugins/google_adid

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.gid
        pluginClass: GidPlugin
